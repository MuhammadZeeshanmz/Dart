void main() {
  int x = 39;
  int y = 20;

  bool result;

  // and  && // tow condition find the realtionship on the basis result
  // or operator ||
  // not operator !
  // the result of this in the boolean

/*
true+true = true
true+false = false
false+true = false
fasle+false = false
*/
// And operator

  // result = (x > y && y < x);

  // result = (x > y && x < y);

// Or operator

  // result = (x < y || x > y);

// Not operator

  // result = !(x == y);
  // result = !(x < y);
    result = !(x > y);
  print(result);
}
